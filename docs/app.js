const STORAGE='parking-pejam-demo-v3';
const LANG_KEY='parking-pejam-lang';
const zones=['A','B','C'];
const now=()=>new Date().toISOString();
const el=id=>document.getElementById(id);
const dictionaries={
 en:{brandSub:'OPERATIONS PLATFORM',source:'Source',eyebrow:'PARKING OPERATIONS · LIVE DEMO',heroTitle:'One control center.<br><span>Every parking decision.</span>',heroCopy:'A polished browser demo of the Parking Pejam platform. Click spaces, run a live simulation, inspect the audit trail and export reports — no installation required.',demoMode:'Demo mode',localOnly:'Local browser state',noInstall:'No installation',runSimulation:'Run live simulation',stopSimulation:'Stop live simulation',reset:'Reset',liveDemo:'Live demo',simulatedInBrowser:'simulated in your browser',reports:'Reports',printPdf:'Print / PDF',liveMap:'LIVE MAP',parkingLayout:'Parking layout',auditTrail:'AUDIT TRAIL',recentActivity:'Recent activity',occupancy:'Occupancy',zoneUtilisation:'ZONE UTILISATION',liveDistribution:'Live distribution',productReady:'PRODUCT READY',builtForDeployment:'Designed for real deployment',productCopy:'The production platform combines ASP.NET Core, EF Core, SQLite, Docker, API endpoints, audit logging and a responsive PWA-ready interface.',reportCenter:'REPORT CENTER',exportAnalysis:'Export & analysis',reportCopy:'Download the current demo state for Excel/analysis or generate a print-ready PDF report.',spotsReport:'Parking spots',eventsReport:'Audit events',snapshotReport:'Full snapshot',printableReport:'Printable report',close:'Close',showcase:'Product showcase',viewGithub:'View on GitHub',allStatuses:'All statuses',free:'Free',occupied:'Occupied',reserved:'Reserved',outOfService:'Out of service',searchPlaceholder:'Search spot, e.g. A-01',allZones:'All zones',zone:'Zone',totalSpots:'Total spots',available:'Available',visible:'visible',health:'● OK',noMatches:'No parking spots match the current filters.',noActivity:'No demo activity yet. Click a space or start the simulation.',operator:'operator',simulation:'simulation',changed:'changed'},
 de:{brandSub:'PARKBETRIEBSPLATTFORM',source:'Quellcode',eyebrow:'PARKBETRIEB · LIVE-DEMO',heroTitle:'Eine Leitstelle.<br><span>Jede Parkentscheidung.</span>',heroCopy:'Eine interaktive Browser-Demo der Parking-Pejam-Plattform. Stellplätze anklicken, Live-Simulation starten, Audit-Protokoll prüfen und Berichte exportieren – ganz ohne Installation.',demoMode:'Demo-Modus',localOnly:'Lokaler Browserzustand',noInstall:'Keine Installation',runSimulation:'Live-Simulation starten',stopSimulation:'Live-Simulation stoppen',reset:'Zurücksetzen',liveDemo:'Live-Demo',simulatedInBrowser:'im Browser simuliert',reports:'Berichte',printPdf:'Drucken / PDF',liveMap:'LIVE-KARTE',parkingLayout:'Parkflächenübersicht',auditTrail:'AUDIT-PROTOKOLL',recentActivity:'Letzte Aktivitäten',occupancy:'Auslastung',zoneUtilisation:'ZONENAUSLASTUNG',liveDistribution:'Live-Verteilung',productReady:'PRODUKTREIF',builtForDeployment:'Für den echten Betrieb entwickelt',productCopy:'Die Produktionsplattform kombiniert ASP.NET Core, EF Core, SQLite, Docker, API-Endpunkte, Audit-Logging und eine responsive PWA-fähige Oberfläche.',reportCenter:'BERICHTSZENTRUM',exportAnalysis:'Export & Analyse',reportCopy:'Aktuellen Demo-Stand für Excel/Analyse herunterladen oder einen druckfertigen PDF-Bericht erzeugen.',spotsReport:'Parkplätze',eventsReport:'Audit-Ereignisse',snapshotReport:'Vollständiger Snapshot',printableReport:'Druckbericht',close:'Schließen',showcase:'Produkt-Demo',viewGithub:'Auf GitHub ansehen',allStatuses:'Alle Status',free:'Frei',occupied:'Belegt',reserved:'Reserviert',outOfService:'Außer Betrieb',searchPlaceholder:'Parkplatz suchen, z. B. A-01',allZones:'Alle Zonen',zone:'Zone',totalSpots:'Parkplätze gesamt',available:'Verfügbar',visible:'sichtbar',health:'● OK',noMatches:'Keine Parkplätze passen zu den aktuellen Filtern.',noActivity:'Noch keine Demo-Aktivität. Klicken Sie auf einen Platz oder starten Sie die Simulation.',operator:'Bediener',simulation:'Simulation',changed:'geändert'},
 fa:{brandSub:'سامانه مدیریت پارکینگ',source:'کد منبع',eyebrow:'مدیریت پارکینگ · دمو زنده',heroTitle:'یک مرکز کنترل.<br><span>برای تمام تصمیم‌های پارکینگ.</span>',heroCopy:'یک نسخه نمایشی تعاملی از پلتفرم Parking Pejam. جای پارک را تغییر دهید، شبیه‌سازی زنده را اجرا کنید، رویدادها را ببینید و گزارش بگیرید؛ بدون نصب هیچ نرم‌افزاری.',demoMode:'حالت دمو',localOnly:'وضعیت محلی مرورگر',noInstall:'بدون نیاز به نصب',runSimulation:'اجرای شبیه‌سازی زنده',stopSimulation:'توقف شبیه‌سازی',reset:'بازنشانی',liveDemo:'دموی زنده',simulatedInBrowser:'شبیه‌سازی داخل مرورگر',reports:'گزارش‌ها',printPdf:'چاپ / PDF',liveMap:'نقشه زنده',parkingLayout:'چیدمان پارکینگ',auditTrail:'تاریخچه رویدادها',recentActivity:'آخرین فعالیت‌ها',occupancy:'میزان اشغال',zoneUtilisation:'میزان استفاده از زون‌ها',liveDistribution:'توزیع زنده',productReady:'محصول آماده ارائه',builtForDeployment:'طراحی‌شده برای استفاده واقعی',productCopy:'نسخه اصلی با ASP.NET Core، EF Core، SQLite، Docker، API، ثبت رویدادها و رابط PWA ریسپانسیو ساخته شده است.',reportCenter:'مرکز گزارش',exportAnalysis:'خروجی و تحلیل',reportCopy:'وضعیت فعلی دمو را برای Excel/تحلیل دانلود کنید یا گزارش قابل چاپ PDF بسازید.',spotsReport:'جای پارک‌ها',eventsReport:'رویدادهای سیستم',snapshotReport:'Snapshot کامل',printableReport:'گزارش قابل چاپ',close:'بستن',showcase:'دموی محصول',viewGithub:'مشاهده در GitHub',allStatuses:'همه وضعیت‌ها',free:'خالی',occupied:'اشغال',reserved:'رزرو',outOfService:'خارج از سرویس',searchPlaceholder:'جستجوی جای پارک، مثلاً A-01',allZones:'همه زون‌ها',zone:'زون',totalSpots:'کل جای پارک',available:'خالی',visible:'نمایش داده شده',health:'● سالم',noMatches:'هیچ جای پارکی با فیلتر فعلی پیدا نشد.',noActivity:'هنوز فعالیتی ثبت نشده است. روی یک جای پارک کلیک کنید یا شبیه‌سازی را اجرا کنید.',operator:'اپراتور',simulation:'شبیه‌سازی',changed:'تغییر کرد'}
};
let lang=localStorage.getItem(LANG_KEY)||'en';
const t=key=>(dictionaries[lang]&&dictionaries[lang][key])||dictionaries.en[key]||key;
const makeSpots=()=>zones.flatMap((zone,zi)=>Array.from({length:16},(_,i)=>({id:`${zone}-${i+1}`,spotNumber:`${zone}-${String(i+1).padStart(2,'0')}`,zone,row:Math.floor(i/8)+1,column:i%8+1,status:((i+zi*3)%17===0?'Occupied':(i+zi*5)%19===0?'Reserved':'Free'),updatedAtUtc:now()})));
let state=JSON.parse(localStorage.getItem(STORAGE)||'null')||{spots:makeSpots(),events:[],filterZone:'All',filterStatus:'all',search:'',running:false};
function persist(){localStorage.setItem(STORAGE,JSON.stringify(state))}
const label=s=>t(s==='OutOfService'?'outOfService':s.toLowerCase());
function stats(){const total=state.spots.length;const free=state.spots.filter(x=>x.status==='Free').length;const occupied=state.spots.filter(x=>x.status==='Occupied').length;const reserved=state.spots.filter(x=>x.status==='Reserved').length;const out=state.spots.filter(x=>x.status==='OutOfService').length;return{total,free,occupied,reserved,out,occupancy:total?Math.round(occupied*1000/total)/10}}
function applyLanguage(){document.documentElement.lang=lang;document.documentElement.dir=lang==='fa'?'rtl':'ltr';document.querySelectorAll('[data-i18n]').forEach(n=>{const key=n.dataset.i18n;n.innerHTML=t(key)});document.querySelectorAll('[data-i18n-placeholder]').forEach(n=>n.placeholder=t(n.dataset.i18nPlaceholder));document.querySelectorAll('.lang').forEach(b=>b.classList.toggle('active',b.dataset.lang===lang));render()}
function setLanguage(next){lang=next;localStorage.setItem(LANG_KEY,lang);applyLanguage()}
function renderMetrics(){const s=stats();const cards=[['◉','totalSpots',s.total],['✓','available',s.free],['●','occupied',s.occupied],['◆','reserved',s.reserved],['◔','occupancy',`${s.occupancy}%`]];el('metrics').innerHTML=cards.map(([icon,k,v])=>`<article class="metric"><div class="metric-icon">${icon}</div><div class="metric-label">${t(k)}</div><div class="metric-value">${v}</div></article>`).join('');el('occupancyText').textContent=`${s.occupancy}%`;el('occupancyBar').style.width=`${s.occupancy}%`;el('healthText').textContent=t('health')}
function visibleSpots(){return state.spots.filter(s=>(state.filterZone==='All'||s.zone===state.filterZone)&&(state.filterStatus==='all'||s.status===state.filterStatus)&&(s.spotNumber.toLowerCase().includes(state.search.toLowerCase())))}
function renderZones(){const list=visibleSpots();const groups={};for(const s of list)(groups[s.zone]??=[]).push(s);const html=Object.entries(groups).map(([zone,items])=>`<section class="zone"><div class="zone-title"><span>${t('zone')} ${zone}</span><span>${items.length} ${t('visible')}</span></div><div class="spot-grid">${items.map(s=>`<button class="spot ${s.status==='Free'?'free':s.status==='Occupied'?'occupied':s.status==='Reserved'?'reserved':'out'}" data-id="${s.id}" title="${s.spotNumber} · ${label(s.status)}"><span class="spot-id">${s.spotNumber}</span><span class="spot-status">${label(s.status)}</span></button>`).join('')}</div></section>`).join('');el('zones').innerHTML=html||`<div class="muted">${t('noMatches')}</div>`;el('visibleSummary').textContent=`${list.length} / ${state.spots.length} ${t('visible')}`;document.querySelectorAll('.spot').forEach(b=>b.addEventListener('click',()=>toggleSpot(b.dataset.id)))}
function renderEvents(){el('eventCount').textContent=state.events.length;el('events').innerHTML=state.events.slice(0,18).map(e=>`<div class="event"><div class="event-top"><span class="event-id">${e.spotNumber}</span><span class="event-time">${new Date(e.timestampUtc).toLocaleTimeString()}</span></div><div class="event-detail">${label(e.oldStatus)} → <strong>${label(e.newStatus)}</strong> · ${e.source==='simulation'?t('simulation'):t('operator')}</div></div>`).join('')||`<div class="event"><div class="event-detail">${t('noActivity')}</div></div>`}
function renderZonesBars(){el('zoneBars').innerHTML=zones.map(z=>{const items=state.spots.filter(x=>x.zone===z);const occ=Math.round(items.filter(x=>x.status==='Occupied').length*100/items.length);return`<div class="bar-row"><strong>${t('zone')} ${z}</strong><div class="bar-track"><span style="width:${occ}%"></span></div><span>${occ}%</span></div>`}).join('')}
function renderTabs(){el('zoneTabs').innerHTML=['All',...zones].map(z=>`<button class="tab ${state.filterZone===z?'active':''}" data-zone="${z}">${z==='All'?t('allZones'):`${t('zone')} ${z}`}</button>`).join('');document.querySelectorAll('.tab').forEach(b=>b.addEventListener('click',()=>{state.filterZone=b.dataset.zone;persist();render()}))}
function render(){el('search').value=state.search;el('statusFilter').value=state.filterStatus;renderMetrics();renderTabs();renderZones();renderEvents();renderZonesBars();el('simulateBtn').innerHTML=`<span>${state.running?'■':'▶'}</span> <span>${state.running?t('stopSimulation'):t('runSimulation')}</span>`}
function record(s,old,next,source='operator'){state.events.unshift({id:crypto.randomUUID?crypto.randomUUID():String(Date.now()),spotNumber:s.spotNumber,oldStatus:old,newStatus:next,source,timestampUtc:now()});state.events=state.events.slice(0,100)}
function toggleSpot(id){const s=state.spots.find(x=>x.id===id);if(!s)return;const next=s.status==='Free'?'Occupied':s.status==='Occupied'?'Free':'Free';const old=s.status;s.status=next;s.updatedAtUtc=now();record(s,old,next);persist();render()}
function simulateOnce(){const candidate=state.spots[Math.floor(Math.random()*state.spots.length)];const old=candidate.status;const next=old==='Free'?'Occupied':old==='Occupied'?'Free':old==='Reserved'?'Free':'Occupied';candidate.status=next;candidate.updatedAtUtc=now();record(candidate,old,next,'simulation');persist();render()}
let timer=null;function toggleSimulation(){state.running=!state.running;if(state.running){simulateOnce();timer=setInterval(simulateOnce,2200)}else{clearInterval(timer);timer=null}render()}
function csv(rows){return '\ufeff'+rows.map(row=>row.map(v=>`"${String(v??'').replaceAll('"','""')}"`).join(',')).join('\n')}
function download(name,type,text){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type}));a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)}
function exportSpots(){download(`parking-demo-spots-${Date.now()}.csv`,'text/csv;charset=utf-8',csv([['SpotNumber','Zone','Row','Column','Status','UpdatedAtUtc'],...state.spots.map(s=>[s.spotNumber,s.zone,s.row,s.column,s.status,s.updatedAtUtc])]))}
function exportEvents(){download(`parking-demo-events-${Date.now()}.csv`,'text/csv;charset=utf-8',csv([['SpotNumber','OldStatus','NewStatus','Source','TimestampUtc'],...state.events.map(e=>[e.spotNumber,e.oldStatus,e.newStatus,e.source,e.timestampUtc])]))}
function exportJson(){download(`parking-demo-snapshot-${Date.now()}.json`,'application/json;charset=utf-8',JSON.stringify({generatedAtUtc:now(),language:lang,statistics:stats(),spots:state.spots,events:state.events},null,2))}
function printReport(){const s=stats();const rows=state.spots.map(x=>`<tr><td>${x.spotNumber}</td><td>${x.zone}</td><td>${label(x.status)}</td><td>${new Date(x.updatedAtUtc).toLocaleString()}</td></tr>`).join('');const title='Parking Pejam — '+t('reportCenter');const w=window.open('','_blank');if(!w)return;w.document.write(`<!doctype html><html lang="${lang}" dir="${lang==='fa'?'rtl':'ltr'}"><head><meta charset="utf-8"><title>${title}</title><style>body{font:14px Arial;padding:28px;color:#152033}h1{margin:0}.meta{color:#64748b;margin:6px 0 20px}.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.card{border:1px solid #ddd;padding:12px;border-radius:8px}.card b{display:block;font-size:20px;margin-top:4px}table{width:100%;border-collapse:collapse;margin-top:24px}th,td{border:1px solid #ddd;padding:7px;text-align:${lang==='fa'?'right':'left'}}th{background:#f5f7fa}</style></head><body><h1>${title}</h1><div class="meta">${new Date().toLocaleString()}</div><div class="stats"><div class="card">${t('totalSpots')}<b>${s.total}</b></div><div class="card">${t('available')}<b>${s.free}</b></div><div class="card">${t('occupied')}<b>${s.occupied}</b></div><div class="card">${t('occupancy')}<b>${s.occupancy}%</b></div></div><table><thead><tr><th>Spot</th><th>Zone</th><th>Status</th><th>Updated</th></tr></thead><tbody>${rows}</tbody></table><script>onload=()=>print()<\/script></body></html>`);w.document.close()}
function reset(){clearInterval(timer);timer=null;state={spots:makeSpots(),events:[],filterZone:'All',filterStatus:'all',search:'',running:false};persist();render()}
function openReports(){el('reportDialog').showModal()}
['en','de','fa'].forEach(l=>document.querySelector(`[data-lang="${l}"]`).addEventListener('click',()=>setLanguage(l)));
el('simulateBtn').addEventListener('click',toggleSimulation);el('resetBtn').addEventListener('click',reset);el('printBtn').addEventListener('click',printReport);el('reportBtn').addEventListener('click',openReports);el('closeReport').addEventListener('click',()=>el('reportDialog').close());el('exportSpots').addEventListener('click',exportSpots);el('exportEvents').addEventListener('click',exportEvents);el('exportJson').addEventListener('click',exportJson);el('printFromDialog').addEventListener('click',printReport);el('search').addEventListener('input',e=>{state.search=e.target.value;renderZones()});el('statusFilter').addEventListener('change',e=>{state.filterStatus=e.target.value;renderZones()});applyLanguage();